const initialState = {
    fleetList: [1,2,3]
};

const app = ( state = initialState, action ) => {
    switch( action.type ){
    
        default: 
            return state;
    };
};

export default app;