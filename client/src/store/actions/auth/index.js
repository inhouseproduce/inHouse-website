import axios from 'axios';

export const auth = form => {
    return async (dispatch, getState) => {
        console.log('runing authentication')
        // try {
        //     let request = await axios.post('/auth/', { form });

        // } catch (error) {
        //     throw error;
        // };
    };
};